---
import SearchForm from './SearchForm.astro';
import { categoryPath, collectionLabels, collectionNames, homePath, type Locale } from '../lib/site';
import { siteNav } from '../lib/i18n';

const { locale, showSearch = true } = Astro.props as { locale: Locale; showSearch?: boolean };

const navItems = collectionNames.map((category) => ({
  href: categoryPath(category, locale),
  label: collectionLabels[category][locale],
}));
---

<nav class="flex w-full flex-wrap items-center gap-2.5 xl:flex-nowrap xl:justify-center">
  <a
    href={homePath(locale)}
    class="theme-panel theme-panel-link inline-flex shrink-0 items-center whitespace-nowrap rounded-2xl px-5 py-2.5 text-[15px]"
  >
    {siteNav[locale].home}
  </a>
  {navItems.map((item) => (
    <a
      href={item.href}
      class="theme-panel theme-panel-link inline-flex shrink-0 items-center whitespace-nowrap rounded-2xl px-5 py-2.5 text-[15px]"
    >
      {item.label}
    </a>
  ))}
  {showSearch ? <SearchForm locale={locale} /> : null}
</nav>
